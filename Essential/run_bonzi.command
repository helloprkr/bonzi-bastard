#!/bin/bash
# Quick launcher for BonziBuddy Enhanced Edition

# Get the directory where this script is located
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd "$DIR"

# Launch BonziBuddy
python3 fixed_bonzi.py